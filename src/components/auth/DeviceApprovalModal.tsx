// DeviceApprovalModal is intentionally disabled.
// The cross-device approval flow was removed in favour of a simpler
// localStorage-based session persistence that does not require any
// inter-device communication or popup.

export const DeviceApprovalModal = () => null;
